import { loopFor } from 'search-film/helpers/loop-for';
import { module, test } from 'qunit';

module('Unit | Helper | loop for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loopFor([42]);
  assert.ok(result);
});
